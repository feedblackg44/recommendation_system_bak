function [state, options, optchanged] = ...
    OutputMyFunction(options, state, flag)

% global DealCache
% for i = 1:length(DealCache.values)
%     DealCache(int2str(i)) = containers.Map();
% end
% 
% global CurrentPopulation
% CurrentPopulation = 0;

optchanged = false;
end
